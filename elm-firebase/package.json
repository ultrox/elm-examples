{
  "name": "elm-firebase",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "run-pty run-pty.json",
    "build": "elm-watch make --optimize",
    "dev:elm": "elm-watch hot",
    "dev:server": "web-dev-server --root-dir public --app-index public/index.html --open"
  },
  "devDependencies": {
    "@web/dev-server": "^0.2.5",
    "elm-watch": "^1.1.2",
    "run-pty": "^4.0.3"
  },
  "dependencies": {
    "firebase": "^9.23.0"
  }
}
